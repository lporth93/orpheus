.. _installation:

Installation
============

To install the ``orpheus`` code it should be sufficient to navitage into the corresponding repository and then run
``pip install .``. In case there are any issues with the compliation of the external `C` files make sure that you
have loaded a compiler.