.. _installation:

Installation
============

To install the ``orpheus`` code, it should be sufficient to navigate into the corresponding repository and then run
``pip install .``. In case there are any issues with the compilation of the external `C` files make sure that you
have loaded a compiler.